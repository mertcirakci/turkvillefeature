{
  "devDependencies": {
    "@wix/cli": "^1.0.0",
    "@wix/eslint-plugin-cli": "^1.0.0",
    "eslint": "^8.25.0",
    "prettier": "^3.2.4",
    "react": "16.14.0"
  },
  "scripts": {
    "postinstall": "wix sync-types",
    "dev": "wix dev",
    "lint": "eslint .",
    "format": "prettier --write \"src/**/*.{js,jsw}\"",
    "publish": "wix publish",
    "preview": "wix preview"
  }
}